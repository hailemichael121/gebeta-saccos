import { SceneContainer } from '@/src/components/scene/SceneContainer'

export function SavingsScene() {
  return (
    <SceneContainer
      id="savings-scene"
      title="Money Flow"
      subtitle="Savings momentum visualized as capital streams through the vault."
    />
  )
}
