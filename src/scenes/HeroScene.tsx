import { SceneContainer } from '@/src/components/scene/SceneContainer'

export function HeroScene() {
  return (
    <SceneContainer
      id="hero-scene"
      title="Enter the Financial World"
      subtitle="Scroll forward through clouds and step into the SACCO ecosystem."
    />
  )
}
